# VYRA Master Roadmap

This is the authoritative, living list of every major system in the VYRA build-out, in
execution order. Update the **Status**, **Related commits**, and **Known risks** columns as
work lands — do not let this file drift out of date. See `VYRA_PROJECT_STATE.md` for the
current snapshot (branch, latest commit, blockers, exact next action) and
`VYRA_ARCHITECTURE.md` for how the pieces fit together.

Status legend: `done` / `in-progress` / `not-started` / `blocked`.

| # | Phase | Status | Depends on | Acceptance criteria | Related commits | Known risks |
|---|---|---|---|---|---|---|
| 0 | Repository audit | done | — | Baseline recorded in `VYRA_PROJECT_STATE.md`; no unrelated fixes made during audit | (docs only, this commit) | Audit was grep-based/targeted, not an exhaustive per-file manual read — deeper issues may surface later |
| 1 | Standalone Recognition Runtime | done | Recognition Steg 1-8 (Types/Rules/Normalizer/Merge/Queue/Controller/Mapper/Card) | 242/242 automated cases pass (Node+browser); manual demo pass covers join/like-burst/mixed-priority/stress/pause-resume/stop/clear/destroy; zero console errors; media.js/studio.html untouched | `540eaac` | None currently open |
| 2 | Runtime hardening | not-started | Phase 1 | 500-mixed-event stress test passes; repeated start/stop and mount/clear cycles leave no leaked subscriptions/state; `?recognitiondebug=1` diagnostics mode exists and is provably inert without the flag; `docs/recognition-runtime-report.md` written | — | None yet — work not started |
| 3 | Generic live event adapter contract | not-started | Phase 2 | `recognition-adapter.js`/`recognition-adapter-types.js` define a provider-agnostic connect/disconnect/event-envelope contract; zero TikTok-specific logic inside it; adapter demo shows a fake provider driving the Recognition Runtime end-to-end | — | Must not duplicate or bypass the existing `server.ps1`/`live-client.js` transport — this phase defines the *contract*, Phase 4 does the real TikTok wiring |
| 4 | TikTok LIVE adapter | not-started | Phase 3; existing `tiktok-bridge/bridge.js` transport | Real or simulated TikTok events flow through Normalizer → Recognition Runtime, never touching the Card directly; gift streaks don't double-count; simulation mode works without a live session | — | `tiktok-live-connector` is an **unofficial** library — no official TikTok API exists; document this limitation prominently, do not imply an official integration |
| 5 | Overlay Runtime | not-started | Phase 1 (Recognition Runtime), Phase 8 (VFX, can be stubbed initially) | `window.VyraOverlayRuntime` coordinates layers (background/ambient/recognition/event-fx/multiplier/MVP/campaign/diagnostics) with one owner per animation loop; no duplicate mounts; responsive across 9:16/16:9/square/custom | — | Must not introduce a second competing animation loop alongside `vfx-ticker.js` |
| 6 | Overlay Editor | not-started | Phase 5 | Layer list, visibility/lock toggles, drag/resize/snap/align, safe-area viz, device presets, undo/redo, browser-source URL generation; editor interactions provably inert in exported browser-source mode | — | Existing `studio.html?overlay=1` mode and editor mode share one page — must not let editor-only listeners leak into overlay-output mode (regression risk against everything built so far) |
| 7 | Browser-source delivery | not-started | Phase 6 | Dedicated `overlay.html`/`/overlay/:overlayId` entry serving transparent, tokenized, sanitized overlay output; invalid overlay IDs rejected; no dashboard data exposed | — | Today `overlay.html` is a bare redirect with zero ID/token concept — this is greenfield, not a partial system |
| 8 | VFX Engine integration | not-started | Phase 1, existing VFX Engine (M1/M2/hardening, done) | Semantic event → VFX command routing layer exists; fixed particle budgets; resize/aspect-safe; reduced-motion + low-perf modes; 1,000-command stress test passes | — | VFX engine today only runs behind `?vfxdemo=` flags — integration must not regress the already-hardened M2 lifecycle guarantees |
| 9 | Top Gifter and MVP system | not-started | Phase 1, Phase 8 | Separate ranking engine (not Card Mapper); Top Gifter (session ranking, time window, streaks) and MVP (match/session winner, final reveal) are distinct concepts with distinct visual families | — | Must not let MVP be starved by low-priority Recognition Queue traffic — needs its own priority lane, not a queue item |
| 10 | Match events and multipliers | not-started | Phase 9 | x2/x3 as semantic events; glove/boost configurable; documented event priority policy; match state recoverable after reconnect; idempotent duplicate updates | — | Match/boost visual precedent already exists in legacy Battle MVP/boost-pack widgets (media.js) — reuse conventions, don't duplicate a second incompatible system |
| 11 | Gift Campaigns | not-started | Phase 1 | Campaign CRUD (name/description/window/target/accepted gifts), progress/contributor ranking, milestone + completion events, campaign-specific VFX | — | Do not assume TikTok exposes scheduling via API — separate manually-entered campaign data from any future provider-synced data |
| 12 | Actions and Event Automation | not-started | Phase 1, Phase 4 (for real triggers) | Trigger/action engine with schema validation, cooldowns, loop protection, execution logs, disabled-rule state, test mode; **no `eval`, no user JS execution** | — | An `action-event.js`/`action-runtime.js`/`action-scenes.js` system already exists in the legacy app — audit it before deciding whether to extend vs. replace (rule #4: don't rewrite stable systems without concrete reason) |
| 13 | Sound Engine | not-started | Phase 12 (for automation-triggered playback) | Library, volume/mute, per-event mapping, cooldown, overlap limit, autoplay-safe, OBS-compatible, missing-file fallback | — | `sound-alerts.js` already exists in the legacy app — audit before building a second system |
| 14 | Account/Workspace/Data model | **blocked** | Product decision (see `VYRA_ARCHITECTURE.md` §9) | Formal models for user/workspace/overlay/campaign/action-rule/event-log/session/ranking/asset/token/plan with IDs, ownership, timestamps, versioning, soft-delete, server-side authorization | — | **Architecture conflict documented, not resolved**: today's app is local-first with no backend/database/auth; this phase as specified implies a hosted multi-tenant SaaS backend. Do not silently pick a direction — raise with the user first |
| 15 | Dashboard and navigation | not-started | Phase 14 (for real multi-workspace nav) or can proceed against local-only state if Phase 14 stays local-first | Professional sidebar, responsive, accessible, no click-blocking overlay, loading/empty/error states, consistent design tokens | — | `studio.html`'s nav (`data-view`/`data-extra` buttons + `studio.js`'s `render()`/`go()`) already works and is described as "kept pristine" in prior session work — extend via the established monkey-patch convention, don't rewrite `studio.js` |
| 16 | Analytics | not-started | Phase 14 (for persisted history) — can start with session-local analytics sooner | Session/joins/follows/shares/likes/gifts/coins/unique-gifters/top-gifters/queue-activity/dropped-events/VFX perf/connection quality, with clear provider-vs-derived data labeling | — | No analytics pipeline exists today at all — greenfield |
| 17 | AI features | not-started | Phase 16 (session data to summarize), optional otherwise | Optional, isolated, non-critical-path AI helpers with graceful failure, rate limits, no secret leakage, no silent private-data upload | — | Must never sit on the critical overlay render path — any AI call failure must degrade to a documented non-AI fallback |
| 18 | Performance and reliability audit | not-started | All prior phases with runtime code | 30-min simulated session, 10k raw events, high like volume, repeated gift streaks, disconnect/reconnect, overlay reload, low-end simulation all pass without leaks/freezes; `docs/performance-report.md` written | — | This is where the Phase 0 baseline findings (setInterval/rAF/listener-cleanup inventory) get individually resolved, not before |
| 19 | Security review | not-started | All prior phases | `docs/security-review.md` documents tested protections and remaining risks (never claims "fully secure") | — | Legacy `innerHTML` usage flagged in Phase 0 baseline is the primary known input to this review |
| 20 | Accessibility and responsive QA | not-started | Phase 15 (dashboard), Phase 6/7 (overlay/editor) | Keyboard nav, focus visibility, ARIA, contrast, reduced motion, screen-reader announcements, zoom, mobile dashboard, portrait/landscape overlay, missing-image states, long names, non-Latin text, emoji | — | Recognition Card already ships `role="status"`/`aria-live="polite"` — extend that bar app-wide, don't introduce a second lower bar |
| 21 | Release preparation | not-started | Substantially all prior phases | README + docs/INSTALLATION, DEVELOPMENT, OBS_SETUP, TIKTOK_STUDIO_SETUP, TIKTOK_CONNECTION, DEPLOYMENT, TROUBLESHOOTING, RELEASE_CHECKLIST all exist; `.env.example` with no real secrets; `.env` gitignored | — | No `.env`/secrets currently exist in the repo (confirmed no TikTok API keys anywhere, since `tiktok-live-connector` needs none) — this phase is mostly about documenting the existing local setup, not inventing new secret management |
| 22 | Final acceptance test | not-started | All prior phases | Clean-checkout install → lint → unit → integration → build → full manual walkthrough (30 numbered checks in the original spec) all pass; `docs/final-acceptance-report.md` written, no item marked passed without actually testing it | — | No lint config and no root `package.json` exist today — "run lint"/"install dependencies" steps need a concrete decision (which subproject, if any) before this phase can literally execute as written |

## Next recommended task

**Phase 2 — Runtime Hardening.** Phase 1 is complete and verified; Phase 0's audit is
recorded. Phase 2 has no unresolved product-level blockers (unlike Phase 14) and directly
extends the already-tested Recognition Runtime, so it's the safest next step. See
`VYRA_PROJECT_STATE.md` → "Exact next action" for the specific work items.

## Open decisions requiring the user (not resolved unilaterally)

1. **Local-first vs. hosted multi-tenant SaaS** (blocks Phase 14 onward) — see
   `VYRA_ARCHITECTURE.md` §9.
2. **Lint/test tooling location** — no root `package.json` exists; Phase 22's "run lint"
   step needs a home before it can be literally executed.
3. **Legacy automation/sound systems** (`action-event.js` family, `sound-alerts.js`) already
   exist — Phases 12/13 should extend these, not build parallel systems, pending a quick
   audit confirming they're fit for purpose.
